name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [develop]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore AltHMinimize.csproj

      - name: Build
        run: dotnet build AltHMinimize.csproj -c Release --no-restore -warnaserror

      - name: Format check
        run: dotnet format AltHMinimize.csproj --verify-no-changes --no-restore
